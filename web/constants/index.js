// Base
export const InputMode = Object.freeze({
  TextInput: 'textInput',
  CodeInput: 'codeInput',
  FromFile: 'fromFile',
})

export const ProcessStatus = Object.freeze({
  Idle: 'idle',
  Processing: 'processing',
  Success: 'success',
  Failed: 'failed',
})
