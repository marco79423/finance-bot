// MYM 120 min
// Backtest time: 2020/03/01 - 2024/01/01 Exchange

inputs: Enabled(1), OrderSize(1);

if Enabled = 0 then
	#Return;

vars: LongMALen(65), ShortMALen(14), Len(6), Count(1);

vars: OpenCond(True);
OpenCond = True;

vars: LongMA(0), ShortMA(0);
LongMA = Average(Close, LongMALen);
ShortMA = Average(Close, ShortMALen);

vars: BuyCond(True);
BuyCond = OpenCond;
BuyCond = BuyCond and LongMA > ShortMA;
BuyCond = BuyCond and CountIF(Close > LongMA, Len) > Count;
if BuyCond then begin
	Buy ("LE") OrderSize contracts next bar market;
end;

vars: ShellCond(True);
ShellCond = OpenCond;
ShellCond = ShellCond and LongMA < ShortMA;
ShellCond = ShellCond and CountIF(Close < LongMA, Len) > Count;
if ShellCond then begin
	Sell ("LX") all contracts next bar market;
end;
