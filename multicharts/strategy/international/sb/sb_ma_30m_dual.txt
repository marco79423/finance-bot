// SB 30 min
// Backtest time: 2015/09/01 - 2024/01/01 Exchange
// Compliance: 1


inputs: Enabled(1), OrderSize(1);

if Enabled = 0 then
	#Return;


vars: MALen(35), KBar(25), BBandMultiplier(1.2);
vars: MA(0), BBandUp(0), BBandDown(0);

MA = Average(Close, MALen);
BBandUp = BollingerBand(Close, KBar, BBandMultiplier);
BBandDown = BollingerBand(Close, KBar, -BBandMultiplier);

if MA > BBandUp then begin
	Buy ("LE") OrderSize contracts next bar market;
end;

if MA < BBandDown then begin
	SellShort ("SE") OrderSize contracts next bar market;
end;
