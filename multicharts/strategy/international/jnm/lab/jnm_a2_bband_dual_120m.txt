// JNM 120 min
// Backtest time: 2018/01/01 - 2024/01/01 Exchange
// Compliance: 1


inputs: Enabled(1), OrderSize(1);

if Enabled = 0 then
	#Return;


vars: KBar(70), BBandMultiplier(1.4);
vars: BBandUp(0), BBandDown(0);

BBandUp = BollingerBand(Close, KBar, BBandMultiplier);
BBandDown = BollingerBand(Close, KBar, -BBandMultiplier);

if Close > BBandUp then begin
	Buy ("LE") OrderSize contracts next bar market;
end;

if Close < BBandDown then begin
	SellShort ("SE") OrderSize contracts next bar market;
end;

if MarketPosition = 1 then begin
	if Close < BBandOut then begin
		Sell ("LX") all contracts next bar market;
	end;
end;

if MarketPosition = -1 then begin
	if Close > BBandOut then begin
		BuyToCover ("SX") all contracts next bar market;
	end;
end;
