// TXF 120 min
// Backtest time: 2018/01/01 - 2024/01/01 Exchange
// Compliance: 1

inputs: Enabled(1), OrderSize(1);

if Enabled = 0 then
	#Return;


vars: SmallMALen(12), MidMALen(15), LongMALen(105);

vars: SmallMA(1), MidMA(1), LongMA(1);
SmallMA = Average(Close, SmallMALen);
MidMA = Average(Close, MidMALen);
LongMA = Average(Close, LongMALen);


vars: OpenCond(True);
OpenCond = not (IsTXFSettlementDay and 1200 < Time and Time < 1400);
OpenCond = OpenCond and not (0845 <= Time and Time <= 0847);
OpenCond = OpenCond and not (1500 <= Time and Time <= 1502);

if MarketPosition <= 0 then begin
	if OpenCond and SmallMA > LongMA and MidMA < LongMA then begin
		Buy ("LE") OrderSize contracts next bar market;
	end;
end;

if MarketPosition >= 0 then begin
	if OpenCond and SmallMA < LongMA and MidMA > LongMA then begin
		SellShort ("SE") OrderSize contracts next bar market;
	end;
end;
