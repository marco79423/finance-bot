// ETH 13 min
// Use bar 175
// For swing trading
// Focus on balancing profits and losses.

inputs: Enabled(1), OrderSize(1);

if Enabled = 0 then
	#Return;

vars: OpenCond(True);

if MarketPosition = 0 then begin
	vars: Len(175), PointSum(0), count(0);
	count = Len - 1;
	PointSum = Summation(AbsValue(Close - Close[1]), count) ;
	vars: Efficient(0);
	if PointSum <> 0 then begin
		Efficient = (Close - Close[count]) / PointSum;
	end else begin
		Efficient = 0;
	end;

	vars: BuyCond(True);
	BuyCond = OpenCond;
	BuyCond = BuyCond and crosses_above(Efficient, 0.14);
	if BuyCond then begin
		Buy ( "LE" ) OrderSize contracts next bar market;
	end;

end else begin
	vars: SellCond(True);
	SellCond = True;
	SellCond = SellCond and HighestBar(High, Len) > Len * 0.95;
	if SellCond then begin
		Sell ("LX_Out") all contracts next bar market;
	end;
end;
